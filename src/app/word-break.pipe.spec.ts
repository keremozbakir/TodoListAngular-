import { WordBreakPipe } from './word-break.pipe';

describe('WordBreakPipe', () => {
  it('create an instance', () => {
    const pipe = new WordBreakPipe();
    expect(pipe).toBeTruthy();
  });
});
